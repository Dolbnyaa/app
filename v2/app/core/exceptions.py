"""
Кастомные исключения прикладного (не HTTP) уровня.

Repositories и services ничего не знают про FastAPI и HTTP-коды — они
выбрасывают эти исключения, а роутеры уже сами решают, в какой именно
HTTP-ответ их превратить (см. try/except в каждом роутере).
"""


class NotFoundError(Exception):
    """Запрошенная сущность не найдена."""
